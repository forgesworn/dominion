/** Shared test constants — deterministic values for reproducible tests. */
export const TEST_PRIVKEY_HEX = 'a'.repeat(64); // 32 bytes of 0xaa
export const TEST_PRIVKEY_HEX_B = 'b'.repeat(64); // alternate key
export const TEST_EPOCH_ID = '2026-W09';
export const TEST_TIER = 'family';
